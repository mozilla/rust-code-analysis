[package]
name = "enums"
version = "0.0.1"
authors = ["Calixte Denizet <cdenizet@mozilla.com>"]
edition = "2018"
build = "../build.rs"

[build-dependencies]
cc = "^1.0"
phf_codegen = "^0.8"

[dependencies]
askama = "^0.8"
enum-iterator = "^0.6"
libc = "^0.2"
phf_codegen = "^0.8"
tree-sitter = "^0.6"

[workspace]
members = ["enum-cli"]
